# yaml-language-server: $schema=../schema/rec_lint.schema.json

guideline:
  - message: すべてのテストは test-projects の .rec_lint.yaml とサンプルファイルを使って実現する
  - message: すべてのテストは run 結果の Vec<String> を複数行文字列として完全一致で検証する
  - message: test-projects/rules/ のケースは単一ファイルを指定して --sort rule で検証する
