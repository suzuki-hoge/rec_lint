rule:
  - label: no-raw-types
    type: regex
    keywords:
      - "List[^<]"
      - "Map[^<]"
    message: Use generic types instead of raw types
    include_exts: [.java]

guideline:
  - message: Check null safety
