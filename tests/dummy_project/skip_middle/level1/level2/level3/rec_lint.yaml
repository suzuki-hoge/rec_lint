rule:
  - label: no-raw-types
    type: forbidden_patterns
    keywords:
      - "List[^<]"
      - "Map[^<]"
    message: Use generic types instead of raw types
    include_exts: [.java]

guideline:
  - message: Check null safety
